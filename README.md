To run this project :
Run on your bash terminal this commands:
*npm install, ganache install, truffle install
* Ganache-cli -d 
* On different terminal
* truffle networks --clean
* truffle migrate --reset
*  yarn start
  

